import { useSessionManager } from '../../src/hooks/useSessionManager';

describe('useSessionManager', () => {
  it('should export the hook', () => {
    expect(useSessionManager).toBeDefined();
  });
});